---
# Verify Proxmox host bridge configuration.
#
# Inventory: shared Ansible inventory (pve_hosts group) generated by generate_ansible_inventory.
# Run with:  ./run --test

- name: Verify Proxmox host bridge configuration
  hosts: pve_hosts
  gather_facts: false
  become: true
  tasks:
    - ansible.builtin.include_tasks: tasks/verify-bridge-config.yaml
